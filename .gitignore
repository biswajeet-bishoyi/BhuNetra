# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
venv/
.venv/
env/

# --- Node / frontend ---
node_modules/
dist/
dist-ssr/
*.local
.vite/

# --- Secrets & env ---
# Never commit real credentials. Keep .env.example in sync instead.
.env
.env.*
!.env.example
*.pem
*.key

# --- Generated runtime state ---
# SQLite is rebuilt from the seed script; committing it would ship stale
# officer decisions and audit rows into every clone.
data/bhunetra.db
data/*.db-journal
data/*.db-wal

# Benchmark output is machine-specific (timings, model build) and regenerated by
# backend/tools/run_extraction_benchmark.py.
data/synthetic/extraction_benchmark.json

# --- Editors / OS ---
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# --- Agent working files ---
.claude/
